# The shell is a plain Activity + Service + WebView, but the in-app QR scanner
# (RemotePickerView) pulls in AndroidX CameraX + ML Kit, which require AndroidX.
android.useAndroidX=true
org.gradle.jvmargs=-Xmx2g
